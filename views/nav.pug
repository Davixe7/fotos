nav(class="navbar navbar-default")
  .container-fluid
  
    .navbar-header
      button(type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false")
        span(class="sr-only") Toggle navigation
        span(class="icon-bar")
        span(class="icon-bar")
        span(class="icon-bar")
      a( class="navbar-brand" href="#") FotoFacilito
    
    div(class="collapse navbar-collapse" id="bs-example-navbar-collapse-1")
      ul(class="nav navbar-nav")
        li.active
          a(href="/app") Inicio 
          span.sr-only (current)
        li
          a(href="/app/upload") Subir foto
        li.dropdown
          a(href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false") Imagenes #[span.caret]
          ul.dropdown-menu
            li.active #[a(href="#") Link]
            li #[a(href="#") Link]
            li #[a(href="#") Link]
          
      //form(class="navbar-form navbar-left")
        //.form-group
        //  input(type="text" class="form-control" placeholder="Search")
        //button(type="submit" class="btn btn-default") Submit
		
      ul(class="nav navbar-nav navbar-right")
        li
          a(href="/logout") #[span.glyphicon.glyphicon-log-out] Salir